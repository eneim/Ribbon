<?xml version="1.0" encoding="utf-8"?>
<resources>
  <!-- styles for the 'BottomNavigation' View -->
  <declare-styleable name="BottomNavigationView">
    <attr format="reference" name="bbn_entries"/>
    <attr format="string" name="bbn_badgeProvider"/>
  </declare-styleable>

  <!-- styles for the menu resource -->
  <declare-styleable name="BottomNavigationMenu">
    <!-- menu default background color -->
    <attr name="android:background"/>

    <!-- default badge color -->
    <attr format="color" name="bbn_badgeColor"/>

    <!-- animation duration for the menu items -->
    <attr format="integer" name="bbn_itemAnimationDuration"/>

    <!-- ripple selector color -->
    <attr format="color" name="bbn_rippleColor"/>

    <attr format="color" name="bbn_itemColorActive"/>

    <attr format="color" name="bbn_itemColorInactive"/>
  </declare-styleable>

  <!-- attributes for the 'item' tag within the menu resource. -->
  <declare-styleable name="BottomNavigationMenuItem">
    <!-- The ID of the item. -->
    <attr name="android:id"/>

    <!-- The title associated with the item. -->
    <attr name="android:title"/>

    <!-- The icon associated with this item.  This icon will not always be shown, so
         the title should be sufficient in describing this item. -->
    <attr name="android:icon"/>

    <!-- Whether the item is enabled (not currently used). -->
    <attr name="android:enabled"/>

    <!-- background color for the selected tab. If omitted, or 0, the background
     color will not change and the BottomNavigationMenu#background will be always used -->
    <attr name="android:color"/>
  </declare-styleable>

  <!-- style for the scroll behavior -->
  <declare-styleable name="BottomNavigationBehavior">
    <attr format="integer" name="bbn_animationDuration"/>
    <attr format="boolean" name="bbn_scrollEnabled"/>
  </declare-styleable>

  <declare-styleable name="ActionTabView">
    <attr format="color" name="colorActive"/>
    <attr format="color" name="colorInactive"/>
  </declare-styleable>
</resources>